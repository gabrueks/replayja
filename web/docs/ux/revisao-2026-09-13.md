# Revisão de UI/UX — Replay já 2.0 · 13 de setembro de 2026

Revisão de **consistência e usabilidade** do visual v2 "Luz de quadra", tela a
tela, contra `design/v2/README.md`, `web/docs/design-system.md` §12, o roteiro de
E2E do `web/README.md` §9 e o `docs/PRD.md`.

**Modo relatório: nenhuma linha do app foi alterada.** Só esta pasta
(`web/docs/ux/`) foi escrita. Nada foi escrito no banco de produção — nenhum
formulário foi enviado, nenhum convite foi gerado, nenhum botão virtual foi
apertado.

## Como foi feito

| | |
|---|---|
| **Onde** | `https://replayja.com.br` (produção, deploy `dpl_4FWGuyR…`) |
| **Telas públicas** | navegador emulando **390 × 844** e **1280 × 900**, anônimo |
| **Telas logadas** | navegador do Gabriel, sessão viva, **430 × 932** (iPhone 15 Pro Max) |
| **Painel** | `web/docs/capturas/v2/painel-*-390.png` e `-1280.png` — a conta usada não é `admin` de nenhuma arena, então o painel foi lido pelas capturas e pelo código |
| **Catálogo** | `/dev/ui` rodando local (`pnpm dev`, porta 3111) — **contra a árvore de trabalho, que já está à frente da produção** |
| **Código** | `web/app/**`, `web/components/ui/**`, `web/lib/**` |

> **A árvore de trabalho está à frente do que está no ar.** Dois agents estão
> corrigindo código agora: `components/ui/Voltar.tsx`, `BottomNav`, `InviteSheet`,
> `/app/lances` e `/[arenaSlug]/page.tsx` já têm mudanças não commitadas. Tudo o
> que está listado abaixo foi **confirmado em produção**, e cada achado diz se a
> correção já está em voo. Os seis bugs que o fundador já reportou não estão na
> lista — mas onde o mesmo padrão se repete em outra tela, ele está (achados
> **P1-7**, **P1-11**, **P1-16**, **P1-18** e **P2-30**).

---

## Contagem

| Prioridade | Quantos |
|---|---|
| **P0** — bloqueia o uso | **4** |
| **P1** — atrapalha | **19** |
| **P2** — polimento | **17** |
| **Total** | **40** |

---

## P0 — bloqueia o uso

| | # | Tela | O que acontece / repro | Causa provável | Correção |
|:--:|---|---|---|---|---|
| ✅ **P0-1** | `/[arena]` e `/[arena]/s/[slug]`, **deslogadas**, 390 × 844 | O CTA fixo de 115px cobre o fim da página **e a página não rola o suficiente para escapar**. Na sessão, a barra **"Manda pro grupo"** inteira (WhatsApp + os 3 ladrilhos) e a frase "Quem abrir o link vê que a pelada existe" ficam permanentemente debaixo do CTA, com o scroll no fim. Na arena, o título "Seus lances estão aqui." e os dois parágrafos do gate ficam cortados. Medido: `padding-bottom` do `main` = **28px**, altura do CTA = **115px**. Repro em `capturas/README.md` P0-1. | `app/globals.css:507` define `.com-cta { padding-bottom: 116px }`, mas os módulos de página usam o **shorthand** `padding` — `app/[arenaSlug]/parceiro.module.css:4` e `app/[arenaSlug]/s/[sessionSlug]/sessao.module.css:4` (`padding: var(--e-20) var(--e-20) var(--e-28)`). Como o CSS Module carrega depois do `globals.css` e a especificidade é igual, o módulo ganha e a reserva evapora — **silenciosamente e só em algumas rotas** (em `/` a mesma classe funciona). | Trocar o shorthand por `padding: X X 0` + `padding-bottom` nas classes globais, ou (melhor) fazer o `CtaFixo` reservar o próprio espaço com um irmão `aria-hidden` de altura igual, e apagar `.com-cta`/`.com-barra`. Ver **"Padrões a criar"**. |
| ✅ **P0-2** | `/app/buscar` | O campo **FIM** mostra `07:47` e o campo **INÍCIO**, do lado, mostra `07:17 AM`. Os dois são `type="time"` com 112px de largura: o FIM está em **24px Bricolage** e o AM/PM **não cabe — some sem nenhum aviso**. Dá para mandar uma busca das 07:47 achando que é 19:47, e o resultado vazio é indistinguível de "não gravou" — que é exatamente o "meio bugado" que o §9 do README diz ter sido consertado. | `components/ui/TimeRangePicker.module.css:71` — `.caixa:first-child .entrada { font-size: 16px; font-family: corpo }` foi escrita para pegar **só a DATA**, mas a caixa do **INÍCIO** também é `:first-child` (do `.linha`, `TimeRangePicker.tsx:181`). Resultado: DATA e INÍCIO em 16px Archivo, FIM em 24px display. | Trocar `:first-child` por uma classe explícita (`.caixaData`) e **dar a mesma tipografia aos dois horários**. Enquanto o nativo for 12h em algum aparelho, os blocos precisam de largura para o sufixo. |
| ✅ **P0-3** | `/app/buscar` | A data sai **`09/13/2026`** — formato americano. É o print do fundador. Não é bug do valor (o `value` é `2026-09-13` e o servidor recebe certo): `<input type="date">` renderiza no locale da **interface do navegador**, e `<html lang="pt-BR">` não muda isso. Qualquer celular/Chrome que não esteja em português mostra mês antes do dia — e o atleta que "buscou dia 13/09" pode ter buscado 9 de dezembro. | `components/ui/TimeRangePicker.tsx:174`, único `type="date"` do produto. O comentário em `TimeRangePicker.module.css:45-53` documenta a premissa errada: *"respeita o formato do aparelho (pt-BR mostra 8/9/2026 e 24h sozinho)"*. | Mínimo: um rótulo em pt-BR ao lado do campo (`sáb, 13 de set`) derivado do `value` — o dado que a pessoa confere deixa de depender do navegador. Definitivo: `CampoDeData` próprio (ver **Padrões a criar**), que é o mesmo componente que resolve o P0-2. |
| ✅ **P0-4** | qualquer URL inválida (`/pagina-que-nao-existe`, arena errada, grupo apagado, clipe expirado) | A 404 é a **página crua do Next**: fundo **preto**, texto **em inglês** ("This page could not be found."), sem marca, sem barra, **sem nenhum link de volta**. Num produto claro em pt-BR cujo canal de distribuição é link colado no WhatsApp, é a tela que mais gente vai ver por engano. O `theme-color` continua `#F6F3EF` numa página preta, e o "Pular para o conteúdo" do layout aponta para um `#conteudo` que não existe ali. | Não existe **nenhum** `not-found.tsx` em `web/app/**`. São **17 chamadas de `notFound()`** em 9 rotas: `[arenaSlug]/page.tsx:136`, `[groupSlug]/page.tsx:134,140`, `s/[sessionSlug]/page.tsx:138,141,144,154,166`, `c/[clipId]/page.tsx:89,96,99,103`, `[groupSlug]/editar/page.tsx:59,63,68,76`, `grupos/novo/page.tsx:52,63`. | `app/not-found.tsx` com `EmptyState` + ilustração `apito` + "Ver as arenas". O padrão já existe e é bom: `app/convite/[token]/page.tsx:131-147` (`<ConviteInvalido />`). É copiar dali. |

---

## P1 — atrapalha

| | # | Tela | O que acontece / repro | Causa provável | Correção |
|:--:|---|---|---|---|---|
| ✅ **P1-5** | todo o app | Não existe **nenhum** `loading.tsx` nem `error.tsx`. As rotas mais pesadas são `force-dynamic` com 9 awaits de banco (`[arenaSlug]/page.tsx`, `[groupSlug]/page.tsx`) — no 4G da quadra a tela anterior fica congelada até o servidor responder, e qualquer exceção de banco vira **tela branca sem "tentar de novo"**. | ausência de arquivos em `web/app/**` | `app/loading.tsx` com o esqueleto do card e `app/error.tsx` com "Deu ruim aqui, não foi você" + botão `reset()`. Um par por área já resolve (`app/app/`, `app/[arenaSlug]/`). |
| ✅ **P1-6** | `/app/buscar`, resultado vazio | **"A câmera da todas as quadras estava lá"**. O artigo está colado no nome do filtro. | `app/app/buscar/page.tsx:205` — `` `A câmera da ${nomeDaQuadra} estava lá…` `` com `nomeDaQuadra = "todas as quadras"`. | Duas frases, não uma interpolada: "A câmera estava lá" quando é `todas`, "A câmera da Quadra 1 estava lá" quando é uma só. |
| ✅ **P1-7** | `/app/buscar`, **0 lances** | Abaixo do estado vazio aparecem **"Manda pro grupo"** e **"Joga toda semana? Vira grupo"** — compartilhar uma busca que não achou nada. É o mesmo padrão dos "dois CTAs iguais em Seus lances" que o fundador reportou, numa tela diferente: **três** ações empilhadas (Trocar de arena + as duas pontes) e nenhuma delas é a que a pessoa quer (mudar o horário). | `app/app/buscar/page.tsx:207-240` — o bloco `pontes` só depende de `pediuBusca && janelaValida`, nunca de `clipes.length`. | Esconder as pontes quando `clipes.length === 0`, e pôr no `EmptyState` os **horários vizinhos** (ver P1-20). |
| ✅ **P1-8** | `/arena-vasco`, deslogada | O contador diz **"0 lances gravados hoje"** e logo abaixo a grade borrada mostra **quatro** cards — `20:47`, `20:51`, `21:03`, `21:12`, todos rotulados **"Hoje · Society"** e perfeitamente legíveis na árvore de acessibilidade e no HTML. Sem nenhuma tarja de exemplo. Quem passa o dedo vê "0" e vê 4; quem lê com leitor de tela ouve quatro lances de hoje que não existem. É o inverso exato da decisão registrada em `design-system.md` §12.5 nº 2 ("duas contagens da mesma coisa sempre divergem"). | `components/ui/LoginGate.tsx` monta a prévia com a fixture de `lib/fixtures.ts`; o `ClipGrid` borrado é `aria-hidden` **no catálogo** (`/dev/ui` diz "BORRADO (DECORATIVO, ARIA-HIDDEN E INERT)") mas na página o texto continua no DOM. `AvisoDeExemplo` existe em `components/ui/` e não é usado aqui. | Ou a prévia vira uma peça **sem texto** (silhuetas), ou ganha a tarja `AvisoDeExemplo`. E conferir que `aria-hidden` + `inert` chegam mesmo na rota, não só no catálogo. |
| ◑ **P1-9** | `/[arena]/s/[slug]` | Numa sessão de **sábado, 12 set**, o contador diz **"0 lances gravados hoje"**. A palavra "hoje" veio junto com o componente da arena e não faz sentido numa janela de outro dia. | `app/[arenaSlug]/s/[sessionSlug]/page.tsx:278-280` reusa o `.contador` de `parceiro.module.css` com a mesma string. | "0 lances nesta pelada" — o rótulo acima já diz "LANCES DESTA PELADA". |
| ✅ **P1-10** | `/[arena]/[grupo]/editar` | **"Última mudança 13 de set., 10:45"** com o relógio da arena em 07:45. Três horas à frente: está em **UTC**. O mesmo vale para "vale até 27 de set., 10:36" nos convites. Num produto cujo dado central é horário, a tela do dono mostra a hora errada. | `app/[arenaSlug]/[groupSlug]/editar/page.tsx:49` — `new Intl.DateTimeFormat("pt-BR", {…})` **sem `timeZone`**, em Server Component (roda em UTC na Vercel). Usada em `:134` e `:190`. | Passar `timeZone` da arena. Existe `lib/fuso.ts` fazendo exatamente isso e certo — o problema é que 8 arquivos formatam por fora dele (ver P2-31). |
| ✅ **P1-11** | `/app/grupos` | A aba **Grupos** não tem **nenhuma** forma de criar um grupo: no lugar do botão há um parágrafo explicando o caminho ("abre a arena e busca o horário da pelada — o botão *Joga toda semana? Vira grupo* já leva…"). É o mesmo beco do `grupos/novo` que o fundador reportou, do outro lado: a tela do objeto não deixa criar o objeto. | `app/app/grupos/page.tsx` — o rodapé é `<p>`, não `Button`. | Um `Button` "Criar o grupo da minha pelada" levando a `/app` (escolher arena) com a explicação **abaixo** dele. A frase já existe, na aba Grupos da arena. |
| ✅ **P1-12** | `/app` | Logo abaixo do card da **Arena Vasco**, a seção "TODAS AS ARENAS" mostra o estado vazio **"Nenhuma arena por aqui ainda / Assim que uma arena publicar a página dela, ela aparece aqui"** — com uma arena publicada visível 40px acima. Lê como bug. | A consulta de "todas" exclui as arenas já listadas em "você jogou aqui", mas o estado vazio não sabe disso. | Enquanto só existir uma arena, esconder a seção inteira. Quando houver mais, o texto certo é "Só a Arena Vasco por enquanto". |
| ✅ **P1-13** | `/[arena]/[grupo]` | A **mesma** contagem aparece duas vezes na mesma tela com palavras diferentes: **"3 na pelada"** (ao lado dos avatares) e **"3 pessoas"** (título de MEMBROS). Na página da arena, o mesmo número vira **"1 membro"**. | `components/ui/MemberAvatars.tsx:74` (`${contagem} na pelada`) contra `app/[arenaSlug]/[groupSlug]/page.tsx:431` e `app/[arenaSlug]/page.tsx:385`. O `MemberAvatars` aceita uma prop `rotulo` que resolveria isso e **nenhum dos 3 call-sites a usa**. | Escolher **uma** palavra e repetir (a folha de voz da v2 manda "na pelada") e passar `rotulo` onde o contexto pedir outra. |
| ✅ **P1-14** | `/[arena]/[grupo]` e `/editar` | A lista de membros mostra o **e-mail completo** de todo mundo (`lucasfaraht@gmail.com`, `bolzi.gabriel@gmail.com`) para qualquer membro do grupo, e a tela de editar mostra ainda o e-mail de quem recebeu cada convite. O produto diz em `/app/perfil` que guarda o e-mail "para saber a quem mostrar os lances" — expor a lista para a turma inteira é outra coisa, e a `docs/legal/analise-lgpd.md` merece ser consultada antes. | `app/[arenaSlug]/[groupSlug]/page.tsx` (seção MEMBROS) e `editar/page.tsx` (QUEM ESTÁ NO GRUPO / CONVITES VALENDO) | Mostrar o **nome** quando existir e o e-mail mascarado quando não (`luc…@gmail.com`), com o endereço completo só para o dono na tela de editar. Decisão de produto — está aqui como pergunta, não como ordem. |
| ✅ **P1-15** | `/app/botao` | A tela-herói escura começa em **y = 62px**: acima dela fica a faixa clara do `app/app/layout.tsx` (logo + avatar), e o `theme-color` da rota é **`#0F1419`**. Ou seja, no Android a barra de status é pintada de escuro e logo abaixo vem uma faixa `#F6F3EF` — **exatamente a "emenda" que o `design-system.md` §12.2 diz que o `themeColor` por rota existe para evitar**, invertida. | `app/app/layout.tsx:29` renderiza o cabeçalho para todas as rotas de `/app/**`; `app/app/botao/page.tsx` tem `viewport.themeColor` escuro. | Esconder o cabeçalho claro em `/app/botao` (a rota já esconde a `BottomNav` por `esconderEm`) e deixar o `.noite` encostar no topo. |
| ✅ **P1-16** | todo o app | **Uma ação, muitas frases** — é a regra nº 3 da folha de voz, e ela está solta. Achar um lance: "Entrar pra ver meus lances" (`/`, gate), "Bora achar meu lance" (`/` logado), "Bora achar seu lance" (`/app`, `/app/lances`, aria da lupa), "Buscar por horário" (`/app/lances`), "Buscar lances" (`<title>`). Criar grupo: "Joga toda semana? Vira grupo" (busca), "Criar" (sessão), "Criar o grupo da minha pelada" (arena), "Vira grupo" (faixa). Trocar de arena: "Trocar arena" (cabeçalho) e "Trocar de arena" (estado vazio) **na mesma tela**. Estado ao vivo: "Gravando agora" (`ArenaCard`) e "AO VIVO" (`/app/botao`). O objeto grupo: "Grupos" (aba), "Suas peladas" (título), "1 pelada salva" (perfil), "1 GRUPO" (lista), "turma" (subtítulo). | strings espalhadas por 20+ arquivos; não existe dicionário. | `lib/copy.ts` com **uma constante por ação** e o teste que proíbe a string crua nas telas. Ver **Padrões a criar**. |
| ✅ **P1-17** | 5 rotas | O `<title>` da aba fala uma língua e a tela fala outra: "Buscar lances" × "Bora achar seu lance"; "Meus grupos" × "Suas peladas."; "Editar grupo" × "Arrumar o grupo"; "Seu perfil" × "Você"; "Painel do parceiro" × "Painel". Quem tem 8 abas abertas no celular lê só o `<title>` — e ele é a v1. | `metadata.title` de cada `page.tsx` | Alinhar com a tela. O `<title>` é a primeira microcópia do produto. |
| ✅ **P1-18** | `/[arenaSlug]` | A página pública **mais importante** do produto (é o endereço que a arena divulga) **não tem `<h1>`** — o nome da arena é um `<span>` dentro do `PartnerHeader` (`PartnerHeader.tsx:92`) e todo o conteúdo começa em `<h2>`. E ela **não tem voltar nem barra**: quem chega pelo WhatsApp e já está logado não tem caminho para `/app`. É o mesmo beco do "voltar de grupo" e do "X do player" que o fundador reportou, na tela de cima. | `app/[arenaSlug]/page.tsx:206-253` — o `PartnerHeader` **tem** a prop `voltar` (`PartnerHeader.tsx:83`, renderizada em `:110`) e a página **nunca a passa**. | Passar `voltar` (o componente `Voltar` já está sendo criado nesta leva) e promover o nome da arena a `<h1>`. |
| ◑ **P1-19** | `/[arenaSlug]` aba **Sobre** | A aba que deveria transformar a página em **landing do parceiro** (PRD) não tem **telefone, WhatsApp, Instagram nem endereço**: mostra "São Paulo, SP" (cidade, sem rua) e, como contato, **`contato@replayja.com.br`** — o e-mail do *Replay já*, rotulado "Contato do piloto". Pior: o schema já guarda `whatsapp`, `telefone` e `instagram` (`app/painel/_components/MarcaEPagina.tsx:401`), e a página os renderiza como **texto morto dentro de `<li>`**, sem `wa.me:`, sem `tel:`, sem link. | `app/[arenaSlug]/page.tsx:414-434` | Transformar cada contato no link certo por `kind` e pedir no cadastro o endereço da arena. Sem isso, "a arena que paga" continua sem o que ela compra. |
| ⏳ **P1-20** | `/app/buscar` e `/app/lances`, vazios | O `EmptyState` do catálogo tem o bloco **"Tem lance por perto"** com os horários vizinhos em pílulas (`Ontem · 20:00–21:00 · Quadra 3 · 14 lances`) — é a decisão registrada na v2. **Nenhuma tela em produção passa `vizinhos`.** O atleta que não achou o lance recebe só "Trocar de arena", que quase nunca é o que resolve. | `components/ui/EmptyState.tsx` suporta; `app/app/buscar/page.tsx:202-212` e `app/app/lances/page.tsx` não usam. | Consultar os horários vizinhos com lance na mesma arena e passar. É o achado com melhor relação entre esforço e alívio de suporte. |
| ✅ **P1-21** | `/[arena]` e `/[arena]/[grupo]`, **logadas** | A página aplica `com-barra` (76px de reserva para a barra inferior) mas **não existe barra inferior fora de `/app/**`** — sobram 76px mortos no pé. E, do outro lado, o atleta logado nessas telas fica sem o chassi de navegação. | `app/[arenaSlug]/page.tsx:211`, `[groupSlug]/page.tsx:230`, `s/[sessionSlug]/page.tsx:216` — `${sessao ? "com-barra" : "com-cta"}` | Ou renderizar a `BottomNav` nessas rotas quando há sessão (coerente com "o chassi está sempre lá"), ou parar de reservar espaço. Hoje é o pior dos dois. |
| ✅ **P1-22** | toasts | O comentário em `components/ui/Toast.tsx:25-26` promete que *"o toast de erro sobe para `assertive`"*. A implementação (`:108`) tem **uma única região `polite`** e o `tom` do item nunca a altera. "Sem conexão" e "O e-mail não saiu" chegam atrasados ou não chegam ao leitor de tela. | `components/ui/Toast.tsx:108` | Duas regiões (`polite` e `assertive`) e o item entra na certa pelo `tom`. |
| ✅ **P1-23** | folha **Convidar** | `components/ui/InviteSheet.module.css:66` tem `outline: none` **incondicional** no `<input readOnly>` do link, e o `.linkCaixa` **não** tem `:focus-within` — ao contrário de `Input.module.css:33` e `TimeRangePicker.module.css:34`, que é o padrão da casa. Esse input é o **primeiro focável** do `<dialog>`, ou seja, o que recebe o foco de `showModal()`: abrir o convite pelo teclado deixa o foco **invisível**. Some também no alto contraste do Windows. | `InviteSheet.module.css:60-70` | Tirar o `outline: none` e pôr o anel no `.linkCaixa:focus-within`, igual aos outros dois campos. (O arquivo está sendo mexido nesta leva — vale conferir junto.) |

---

## P2 — polimento

| | # | Tela | O que acontece | Causa provável | Correção |
|:--:|---|---|---|---|---|
| ⏳ **P2-24** | Painel, Visão geral | **"cobertura 24 h 85.2%"** e **"0.0%"** — decimal com **ponto**, à moda americana — três blocos abaixo de **"média de 0,6 por dia"**, com vírgula. Duas convenções numéricas na mesma tela. | `lib/saude-visao.ts:102` — `` `${(v*100).toFixed(casas)}%` ``. Não há **nenhum** `Intl.NumberFormat` no repositório: milhar nunca é separado ("1234 lances"). | `Intl.NumberFormat("pt-BR")` num helper único. |
| ⏳ **P2-25** | Painel no celular (390) | A faixa de abas do topo é cortada no meio da palavra — "**Botõe**" — sem gradiente, sombra ou seta indicando que rola. | `app/painel/painel.module.css` (faixa de abas sem `--faixa-recuo`/máscara) | Aplicar o mesmo tratamento da `ChipFaixa`, que já resolve isso do lado do atleta. |
| ⏳ **P2-26** | Painel no celular | "Relay **relay**-1: último sinal há 0s" (a palavra duplicada, o id cru na cara do parceiro); o título "Gravação por quadra" quebra em duas linhas e disputa espaço com dois selos à direita; "Configurar câmera" é a ação principal de cada card e é um link de texto sem alvo declarado; e o painel inteiro não tem barra inferior no celular, ao contrário de todas as telas do atleta. | `app/painel/page.tsx`, `_components/Quadras.tsx`, `painel.module.css` | O painel ficou fora do escopo da v2 (`design-system.md` §12.1); vale uma leva própria. |
| ✅ **P2-27** | `/app/buscar` | **"30min de busca"** (sem espaço). E para uma janela cheia sai **"2h  de busca"** com espaço duplo. | `components/ui/TimeRangePicker.tsx:214-216` — três expressões concatenadas | Montar a string numa função e usar `"30 min"`, `"2 h"`. |
| ✅ **P2-28** | `/[arenaSlug]` | A aba **Grupos** mostra o badge **"0"**. O `BottomNav` documenta que `0` esconde o badge; o `PartnerHeader` não segue a mesma regra. E no catálogo a aba "Lances" mostra contagem (132) que a página real não mostra. | `components/ui/PartnerHeader.tsx:150` | Esconder quando `0`, mostrar em Lances também — ou em nenhuma das duas. |
| ✅ **P2-29** | `/entrar` e todo campo | O anel de **foco** é `#D93C06` (`--cor-acao`) e o anel de **erro** é `#B5122E` (`--cor-erro`): dois vermelhos-laranja de 2,5px que, num campo de 390px, são o mesmo sinal. No `/entrar` o campo abre **autofocado** e parece um campo com erro antes de a pessoa digitar qualquer coisa. | `components/ui/Input.module.css:33` e `:35`; `TimeRangePicker.module.css:34` e `:35` | Dar ao erro um segundo sinal (ícone + a mensagem já existe) ou afastar os matizes. Estado nunca deve ser só cor — é a regra que o `StatusDot` já respeita. |
| ✅ **P2-30** | várias | Alvos abaixo de 44px, todos ao lado de alvos de 44+: **"Trocar arena"** 75 × 20 (`/app/buscar`), o avatar **"Seu perfil"** 36 × 36 (todo `/app/**`), **"Ver todos"** e **"Limpar"** ≈ 18px (`app/app/arenas.module.css:67`, usados em `app/app/page.tsx:113,145`), o breadcrumb de `/[arena]/grupos/novo` ≈ 18px (`criar-grupo.module.css:14`) — que **é o único voltar daquela rota**, o mesmo beco que está em correção. | tokens existem (`--toque-min: 44px`), esses quatro não os usam | `min-height: var(--toque-min)` + `display: inline-flex; align-items: center`. |
| ✅ **P2-31** | todo o app | **Quatro formatos de data** para a mesma coisa: "Segunda, 14 set às 20:00" (grupo), "seg · 20:00–22:00" (chip), "13 de set., 10:45" (editar), "Sábado, 12 set · 18h–19h" (sessão). Por baixo: **11 cópias** de array de dia da semana em **duas convenções de índice incompatíveis** — `lib/fuso.ts:111` é 0-indexado (domingo primeiro) e `app/[arenaSlug]/[groupSlug]/page.tsx:73` é 1-indexado com `""` no zero, **e as duas se chamam `DIAS`**. Um copy-paste entre elas erra o dia em silêncio. Mais 5 cópias de meses. | `lib/fuso.ts` existe, é bom e cobre só o card de clipe | `lib/datas.ts` central (ver **Padrões a criar**). |
| ◑ **P2-32** | várias | Plurais sem tratamento: **"1 lances em 30 dias"** (`app/painel/_components/Quadras.tsx:142` — na mesma linha, "câmera" e "botão" são pluralizados e "lances" não), **"1 quadras"** (`app/[arenaSlug]/page.tsx:212`, enquanto a linha 226 do mesmo arquivo trata o singular), **"1 dias"** no convite (`lib/email.ts:213,232`). E três telas do painel usam **"lance(s)"**, forma que não existe em nenhuma outra parte do produto (`painel/page.tsx:167`, `Privacidade.tsx:262,340`). | 36 ternários `=== 1 ?` espalhados, nenhum helper | `plural(n, "lance", "lances")` ou `Intl.PluralRules`. |
| ✅ **P2-33** | `ClipCard`, `ArenaCard`, `PartnerHeader` | Toda imagem é `<img>` cru **sem `width`/`height`** (`ClipCard.tsx:60`, `ArenaCard.tsx:65,86`, `PartnerHeader.tsx:87,104`). Hoje não dói porque **não há nenhuma imagem em produção** (a capa é `ArteQuadra` em CSS, a thumbnail é grama desenhada). Quando a capa de 230px do `PartnerHeader` chegar (C9), ela vira o LCP da página pública **sem dimensão reservada e sem prioridade**. | decisão consciente de não usar `next/image` (URL assinada), mas sem as dimensões | `width`/`height` ou `aspect-ratio` no CSS + `fetchpriority="high"` na capa. Barato agora, caro depois. |
| ✅ **P2-34** | desktop 1280 | A `BottomNav` não tem `max-width`: no desktop as quatro abas se espalham por 1280px (320px cada) enquanto a coluna de conteúdo tem 640px centrados. O `CtaFixo` já resolveu isso (`CtaFixo.module.css:15-17`, `.dentro { max-width: 640px }`) — a barra não. | `components/ui/BottomNav.module.css:6-19` | Mesmo `max-width: 640px; margin: 0 auto` de dentro. |
| ✅ **P2-35** | horários | A regra global `app/globals.css:441` é `.tempo, time, .contador` — e **dois dos três seletores são letra morta**: não existe **nenhum** `<time>` no produto (nenhum horário é marcado semanticamente) e `.contador` só existe como classe de módulo (nome hasheado), então a regra global nunca casa. Sem tabular-nums: a lista de horários de `/app/perfil:128-132`, o `.contagem` do `MemberAvatars`, a duração do "melhor da rodada" (`[groupSlug]/page.tsx:311-315`, enquanto o horário irmão na linha 309 tem). | `app/globals.css:441` | Usar `<time datetime>` de verdade (ganha o seletor **e** a semântica) e apagar `.contador` da regra. |
| ✅ **P2-36** | 404 e qualquer rota sem `#conteudo` | O link "Pular para o conteúdo" (`app/layout.tsx`) é a primeira parada do Tab em **toda** página, inclusive na 404 crua, onde o alvo `#conteudo` não existe: o atalho leva a lugar nenhum. | `app/layout.tsx:120-122` + ausência de `not-found.tsx` | Resolvido junto com **P0-4**. |
| ✅ **P2-37** | `/entrar`, `/bem-vindo` | Nenhuma das duas tem volta para `/`. É funil, e é defensável — mas quem clicou em "Entrar pra ver meus lances" por engano na home só sai pelo botão do navegador. | `app/entrar/page.tsx`, `app/bem-vindo/Onboarding.tsx:83-88` | Um `Voltar` discreto no topo do `/entrar` (o componente já está chegando). |
| ✅ **P2-38** | `ClipCard` "cortando" | A transição "Cortando… → pronto" não é anunciada: o card tem `role="group"` + `aria-label` estático (`ClipCard.tsx:118-126`) e, quando vira `<Link>`, nada é falado. `role="group"` num nó sem foco e sem filhos rotulados também não é a semântica certa aqui. | `components/ui/ClipCard.tsx:118-126`, `ClipGrid.tsx:46` | `role="status"` no selo e `aria-live="polite"` no contêiner da grade. |
| ✅ **P2-39** | `/[arena]/[grupo]`, rodada vazia | "Assim que alguém apertar o botão **segunda entre 20:00 e 22:00**" — falta a preposição ("**na** segunda"). | `app/[arenaSlug]/[groupSlug]/page.tsx` (interpolação do dia) | Montar a frase com o dia já preposicionado. |
| ✅ **P2-40** | `/arena-vasco` | "**2 quadras** gravando" (selo), "2 quadras com câmera" (tagline) e "São Paulo, SP · Piloto do Replay já · **2 quadras com câmera**" (aba Sobre) — a mesma informação três vezes na mesma tela, e a linha "ONDE FICA" repete o tagline em vez de dar o endereço. | `app/[arenaSlug]/page.tsx:212,226` e `:414-418` | O selo diz o estado, o tagline diz o que a arena é, "ONDE FICA" diz onde ela fica. |

---

## O que foi medido e **passou**

Vale registrar, porque encurta a próxima revisão:

- **Contraste.** Script de contraste rodado elemento a elemento em `/`, `/entrar`, `/arena-vasco`, `/app` e `/app/buscar`: **zero** pares abaixo de AA (4,5:1 normal / 3:1 grande). O `--cor-texto-3: #726961` e o `--cor-acao-escrita: #C23604` de `design-system.md` §12.4 seguram.
- **Peso e performance.** `/arena-vasco` carrega **~190 KB** no total: dois `woff2` auto-hospedados (34 KB + 21 KB, `preload`), 13 KB de CSS e o JS do Next. TTFB 11 ms, `DOMContentLoaded` 162 ms, `load` 242 ms. **Sem FOUT de rede** (`next/font` + `display: swap`). Não há imagem nenhuma para pesar — o que é ao mesmo tempo o motivo de estar rápido e o **P2-33**.
- **Sem overflow horizontal** em nenhuma das 12 telas visitadas, em 390, 430 e 1280.
- **`aria-label` em botão só com ícone:** os **14** casos do produto têm. Nenhum furo.
- **`rel="noopener noreferrer"`** em todo link externo (`InviteSheet.tsx:136-145`, `ShareBar.tsx:225`).
- **`router.back()`: zero ocorrências** — toda saída é `href` explícito. É a decisão certa e está mantida.
- **Foco visível global** (`app/globals.css:495-498`), **zoom não travado**, **`lang="pt-BR"`**, **alvos de 44px tokenizados**. Só as exceções listadas em P2-30 e P1-23 escapam.
- **Aba ativa da barra inferior** nunca fica nula, e `aria-current="page"` confere em todas as rotas de `/app/**`.

---

## Padrões a criar

Sete padrões cobrem 28 dos 40 achados. São eles que impedem a lista de voltar a crescer.

### 1. `Voltar` — **já está sendo criado nesta leva**
`components/ui/Voltar.tsx` apareceu na árvore de trabalho e já está no `/dev/ui`.
O que falta é **usá-lo onde ainda não tem**: `/[arenaSlug]` (P1-18) e `/entrar`
(P2-37), e trocar o breadcrumb de 18px de `grupos/novo` por ele (P2-30).

### 2. `lib/datas.ts` — uma casa só para data e hora
Hoje há `lib/fuso.ts` (correto, com `timeZone`) **e mais 11 implementações
avulsas**, em duas convenções de índice. O módulo central deve exportar
`diaCurto`, `diaLongo`, `dataCurta`, `dataLonga`, `faixaDeHorario`, todas
recebendo o fuso da arena, e o ESLint deve proibir `toLocale*` e
`Intl.DateTimeFormat` fora dele. Mata P1-10, P2-31 e metade do P2-35.

### 3. `CampoDeData` e `CampoDeHorario`
O nativo foi escolhido por bons motivos (roda do iOS, leitor de tela, zero JS) e
o custo apareceu: formato americano (P0-3) e sufixo AM/PM que não cabe (P0-2). O
caminho barato é manter o `<input>` nativo e **acrescentar um rótulo em pt-BR
derivado do `value`** logo abaixo — a pessoa confere no nosso formato e continua
editando no do aparelho. Os dois campos de horário passam a ter a **mesma**
tipografia, sempre.

### 4. `RodapeFixo` que reserva o próprio espaço
`.com-cta` e `.com-barra` são classes globais que **qualquer `padding` shorthand
de módulo derruba em silêncio** — e derrubam (P0-1). O padrão certo é o próprio
`CtaFixo`/`BottomNav` renderizarem um irmão `aria-hidden` da mesma altura no
fluxo, ou o layout usar `padding-block-end` numa camada que os módulos não
alcancem. Depois disso, apagar as duas classes globais.

### 5. `lib/copy.ts` — a folha de voz virando código
Uma constante por ação (`ACHAR_LANCE`, `CRIAR_GRUPO`, `TROCAR_ARENA`,
`AO_VIVO`, `MEMBROS`), usada em tela **e** em `<title>`, com um teste que falha
se a string crua reaparecer num `page.tsx`. É o que transforma a regra nº 3 da
v2 ("a mesma ação tem UMA frase") em algo que não depende de vigilância. Mata
P1-16, P1-17, P1-13 e P2-39.

### 6. `plural()` e `numero()`
`plural(n, "lance", "lances")` e `numero(v, { casas })` sobre
`Intl.NumberFormat("pt-BR")`. Mata P2-24 e P2-32, e apaga o "lance(s)" do painel.

### 7. As três telas que faltam: `not-found`, `error`, `loading`
`app/not-found.tsx`, `app/error.tsx` e um `loading.tsx` por área. O molde já
existe e é bom: `app/convite/[token]/page.tsx:131-147`. Mata P0-4, P1-5 e P2-36.

---

## Top 10 para a próxima leva

Por ordem de **dano dividido por esforço**.

| | Achado | Por que primeiro |
|---|---|---|
| **1** | **P0-1** — o CTA fixo cobre o fim da arena e da sessão | Deixa o **compartilhar** — o motor de crescimento do produto — inalcançável no celular, na página que a arena divulga. Uma linha de CSS por módulo. |
| **2** | **P0-2** — o campo FIM corta o AM/PM | Faz o atleta buscar 07:47 achando que buscou 19:47 e concluir que "não gravou". É um `:first-child` trocado por uma classe. |
| **3** | **P0-4** — a 404 crua | Fundo preto, em inglês, sem saída, no fim de todo link de WhatsApp que apontar para um grupo apagado ou clipe expirado. Um arquivo novo. |
| **4** | **P0-3** — `09/13/2026` | É o print do fundador e é o tipo de detalhe que derruba a confiança inteira numa demo. Um rótulo derivado do `value` resolve hoje. |
| **5** | **P1-8** — "0 lances" com quatro lances de exemplo do lado | O produto se contradiz na primeira tela que o visitante vê, e o exemplo não está marcado como exemplo. |
| **6** | **P1-11** — a aba Grupos não deixa criar grupo | A tela do diferencial do PRD não tem o botão do diferencial do PRD. Um `Button` no lugar de um `<p>`. |
| **7** | **P1-10** — horário em UTC no editar | Num produto de horário, a tela do dono mostra três horas a mais. Um argumento numa chamada. |
| **8** | **P1-19** — a aba Sobre sem telefone, WhatsApp e endereço | É o que a arena compra. Os campos já existem no banco e no painel; falta virar link. |
| **9** | **P1-6** + **P1-7** — "A câmera da todas as quadras" e as pontes no resultado vazio | Os dois moram no mesmo arquivo e nas mesmas 40 linhas. Meia hora. |
| **10** | **P1-5** — `loading.tsx` e `error.tsx` | É o que separa "está carregando no 4G" de "o app travou" na única rede que a quadra tem. |

Depois desses dez, o próximo passo com maior retorno **não é um bug**: é o
**padrão nº 5** (`lib/copy.ts`). Enquanto cada tela escolher a própria frase, a
lista de P1-16 volta a crescer a cada rota nova — e é o achado que mais faz o
produto parecer gerado, que é exatamente o que a v2 saiu para consertar.

---

## Estado em 13/09, fim do dia

Esta seção foi escrita pela leva que FECHOU os achados. A revisão acima não foi
alterada em mais nada: cada achado ganhou só a marca na primeira coluna.

| | Significa |
|:--:|---|
| ✅ | Fechado, com teste. |
| ◑ | A parte que dava para fechar sem sair da minha área está fechada; o resto tem dono nomeado abaixo. |
| ⏳ | Aberto, com dono. |

| Prioridade | ✅ | ◑ | ⏳ | Total |
|---|--:|--:|--:|--:|
| **P0** | 4 | — | — | 4 |
| **P1** | 16 | 2 | 1 | 19 |
| **P2** | 13 | 1 | 3 | 17 |
| **Total** | **33** | **3** | **4** | **40** |

### Os sete padrões

| # | Padrão | Onde | Achados que ele mata |
|---|---|---|---|
| 1 | `Voltar` | usado em `/[arenaSlug]` e `/entrar` | P1-18, P2-37, P2-30 |
| 2 | `lib/datas.ts` | substituiu as 11 cópias de tabela de dia/mês | P1-10, P2-31, metade do P2-35 |
| 3 | `CampoDeData` / `CampoDeHorario` | `components/ui/CampoDeTempo.tsx` | P0-2, P0-3 |
| 4 | `RodapeFixo` | `CtaFixo` e `BottomNav` reservam o próprio espaço | P0-1, P1-21, P2-34 |
| 5 | `lib/copy.ts` + `lib/copy.test.ts` | a varredura proíbe a frase crua em `app/**` | P1-16, P1-17, P1-13, P2-39 |
| 6 | `lib/plural.ts` | `plural()` e `numero()` sobre `Intl` | P2-32, P2-24 (o helper; o painel ainda não o usa) |
| 7 | `not-found` · `error` · `loading` | `app/**`, mais quatro rotas pesadas | P0-4, P1-5, P2-36 |

E um oitavo que esta leva acrescentou, vindo do README §13: **`Faixa`**
(`components/ui/Faixa.tsx`), a fileira rolável que estava copiada em três
arquivos — a razão de o bug 1 do teste em produção ter aparecido em três telas
de uma vez (UX-6).

### O que ficou aberto, e de quem é

| # | O que falta | Dono |
|---|---|---|
| **P1-20** | Os **horários vizinhos com lance** no estado vazio da busca. O `EmptyState` aceita `sugestoes` desde a v2 e nenhuma tela passa. Falta a consulta — algo como `horariosVizinhosComLance(partnerId, janela, tz)`, devolvendo 3 faixas com contagem. **Não a criei** porque `db/queries/**` não é minha área nesta leva. Enquanto ela não existe, o vazio oferece **"Mudar o horário"** como ação principal, que é o que quase sempre resolve e era o que faltava. | quem cuidar de `db/queries/clipe.ts` |
| **P1-9** | O contador da sessão agora **some** quando a janela não é de hoje, em vez de mentir "0 lances gravados hoje" num sábado. Para ele APARECER com o número certo falta uma contagem pública por janela: `lancesDeHojeNaArena` conta o dia inteiro da arena, e `clipesDaArena` exige login. | idem |
| **P1-19** | Telefone, WhatsApp, Instagram, endereço e mapa **estão fechados**. Falta só **horários de funcionamento**: `partner_branding.opening_hours` existe (migração 0011) e `parceiroPublicoPorSlug` não o projeta. A seção já está escrita em `app/[arenaSlug]/page.tsx` e liga sozinha quando a coluna chegar. | quem cuidar de `db/queries/parceiro.ts` |
| **P2-24** | `lib/plural.ts` entrega `numero()` e `percentual()` com vírgula decimal e ponto de milhar. `lib/saude-visao.ts` e as telas do painel ainda usam `toFixed`. | painel |
| **P2-25** | A faixa de abas do painel cortada no meio da palavra. `components/ui/Faixa.tsx` resolve — é aplicá-lo. | painel |
| **P2-26** | "Relay relay-1", o id cru, "Configurar câmera" sem alvo declarado, e o painel sem barra no celular. | painel |
| **P2-32** | Os plurais do ATLETA estão fechados. As três telas do painel com `"lance(s)"` — forma que não existe em nenhuma outra parte do produto — continuam. | painel |

### E os sete do README §13

| | Estado |
|:--:|---|
| ✅ **UX-1** | A página da arena deslogada ganhou `Voltar`. A barra de quatro abas continua fora do caso deslogado de propósito: as quatro levariam ao login. |
| ✅ **UX-2** | `/app/lances` não adivinha mais a arena. Uma arena → vale; várias → a fileira aparece e a tela espera o toque. |
| ✅ **UX-3** | A fileira de arenas aparece sempre que há arena, e não só com duas ou mais. |
| ✅ **UX-4** | Os três pontos do player viraram uma lupa. |
| ⏳ **UX-5** | `/app/botao` continua com a seta como única saída. É o critério, e ele está atendido — fica registrado como frágil. |
| ✅ **UX-6** | `components/ui/Faixa.tsx`, e o teste proíbe a cópia de voltar aos três arquivos. |
| ⏳ **UX-7** | O cabeçalho do grupo continua apertado em 390 com nome longo. Não mexi: é redesenho, não correção. |

### Verificações

```
pnpm typecheck   ✅
pnpm lint        ✅  0 erros, 0 avisos
pnpm test        ✅  523 passando (baseline da manhã: 332) · 87 pulados
pnpm build       ✅  exit 0
```

**Lighthouse, build de produção servido em `localhost`, perfil mobile:**

| Rota | Performance | Acessibilidade | Boas práticas |
|---|--:|--:|--:|
| `/` | **97** | **100** | **100** |
| `/entrar` | **97** | **100** | **100** |

Os três acima de 90, que era a meta. A categoria **SEO** não tem número
confiável nesta medição: a home REDIRECIONA o visitante de primeira viagem para
`/bem-vindo` (é o `PrimeiraAbertura`, e o redirecionamento é de cliente), então o
Lighthouse acaba medindo o onboarding — que é `noindex` por decisão. O `/` em si
serve `index, follow`, `<title>`, `description`, `viewport`, `lang="pt-BR"` e, a
partir desta leva, `canonical`. A página pública que importa para busca é
`/[arenaSlug]`, e medi-la exige banco.

### Capturas

`web/docs/capturas/v2/ux-*.png`, feitas contra `localhost` **sem banco** — então
cobrem o que não depende dele:

| Arquivo | O que prova |
|---|---|
| `ux-p0-01-home-reserva-390.png` | O conteúdo termina ACIMA do CTA fixo. Medido no navegador: reserva de 115,19px contra barra de 115,19px, com o scroll no fim e 55,7px de folga. |
| `ux-p0-04-nao-encontrado-390.png` | A 404 nova: clara, em pt-BR, com marca e três saídas. |
| `ux-p1-05-carregando-390.png` | O `loading.tsx` da arena — pego por acidente por uma captura sem espera, e por isso mesmo a prova de que ele aparece. |
| `ux-p2-37-entrar-voltar-390.png` | O "×" de saída do `/entrar`, no canto direito. |
| `ux-bem-vindo-390.png` | O onboarding, para o par com o anterior. |

As telas com dado — arena, sessão, grupo, busca — **não foram capturadas**: elas
exigem uma sessão com banco, e a disciplina desta rodada (a mesma da revisão) é
não escrever em produção. Ficam para quem tiver um `.env` de desenvolvimento
apontando para uma branch do Neon.
